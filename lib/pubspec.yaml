name: ativador_app
description: App Ativador

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  permission_handler: ^11.0.1

flutter:
  uses-material-design: true
